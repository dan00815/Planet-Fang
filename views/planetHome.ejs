<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>方的</title>
    <style>
      h1 {
        color: rgba(165, 42, 42, 0.79);
      }
      a {
        text-decoration: none;
      }
      a.create {
        text-decoration: none;
        font-weight: bold;
        color: darkgreen;
        background-color: bisque;
        border: 2px solid white;
        border-radius: 10px;
        padding: 5px;
      }
      a:hover {
        text-decoration: underline;
        background-color: rgba(255, 255, 0, 0.5);
        transition: all ease 0.5s;
      }
    </style>
  </head>
  <body>
    <h1>
      <u><i>歡迎來到克匡的小小植物園</i></u>
    </h1>

    <ul>
      <%basicData.forEach(planet=>{%>
      <li><a href="/planets/<%=planet._id%>"> <%=planet.name%></a></li>
      <pre>（<%=planet.detail_name%>）</pre>

      <%})%>
    </ul>

    <br /><a href="/planets/new" class="create">新增植物</a>
  </body>
</html>
